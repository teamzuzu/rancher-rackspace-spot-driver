apiVersion: management.cattle.io/v3
kind: KontainerDriver
metadata:
  name: rackspacespot
  labels:
    app.kubernetes.io/name: rackspacespot
    app.kubernetes.io/version: {{ trimPrefix "v" .Chart.AppVersion | quote }}
spec:
  displayName: "Rackspace Spot"
  description: "Cluster Driver for Rackspace Spot"
  url: {{ .Values.kontainerDriver.url | quote }}
  checksum: {{ .Values.kontainerDriver.checksum | quote }}
  uiUrl: ""
  active: true
  builtin: false

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: [self-hosted, anchovy]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Generate go.sum
        run: go mod tidy

      - name: Format
        run: go fmt ./...

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m

  tidy:
    name: go mod tidy
    runs-on: [self-hosted, anchovy]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - run: go mod tidy

      - name: Fail if go.mod changed
        run: |
          if ! git diff --quiet go.mod; then
            echo "go.mod is out of date — run 'go mod tidy' locally and commit go.mod"
            git diff go.mod
            exit 1
          fi

  build:
    name: Build
    runs-on: [self-hosted, anchovy]
    strategy:
      matrix:
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Generate go.sum
        run: go mod tidy

      - name: Build (${{ matrix.goarch }})
        env:
          CGO_ENABLED: "0"
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
        run: go build -ldflags "-s -w" -o bin/kontainer-engine-driver-rackspacespot .

  test:
    name: Test
    runs-on: [self-hosted, anchovy]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Generate go.sum
        run: go mod tidy

      - name: Run tests
        run: go test -v -count=1 ./...

  vet:
    name: go vet
    runs-on: [self-hosted, anchovy]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Generate go.sum
        run: go mod tidy

      - run: go vet ./...
